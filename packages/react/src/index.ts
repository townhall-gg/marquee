// React components
export { Marquee, type MarqueeProps, type MarqueeRef } from "./marquee";

// Hooks
export { useMarquee, type UseMarqueeOptions, type UseMarqueeTuple } from "./use-marquee";

// Re-export core types
export type {
  Direction,
  MarqueeInstance,
  MarqueeOptions,
  MarqueeState,
} from "@townhall-gg/marquee";
